import { ConvertToSpaces.Pipe.TsPipe } from './convert-to-spaces.pipe.ts.pipe';

describe('ConvertToSpaces.Pipe.TsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToSpaces.Pipe.TsPipe();
    expect(pipe).toBeTruthy();
  });
});
